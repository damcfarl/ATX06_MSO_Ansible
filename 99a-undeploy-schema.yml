---

  - name: undeploy schema
    hosts: mso1
    connection: local
    gather_facts: no

    vars_files:
    - external_vars.yml

    tasks:
    - name: undeploy schema site 1
      delegate_to: localhost 
      mso_schema_template_deploy: 
        host: "{{ host }}"
        username: "{{ user }}"
        password: "{{ pass }}"
        schema: "{{ schema }}"
        template: "{{ template }}"
        site: "{{ site1 }}"
        state: undeploy
        validate_certs: no
